---
phase: 02-rooms-ranching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/data/rooms.yaml
  - src/data/hatches.yaml
  - src/components/RoomEntry.astro
  - src/pages/base-layouts/index.astro
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03

must_haves:
  truths:
    - "User can find any standard room type and see its min/max tile dimensions"
    - "User can see required buildings for each room type"
    - "User can see room bonus effects and layout tips"
    - "User can see upgrade paths between rooms (Latrine->Washroom, etc.)"
  artifacts:
    - path: "src/data/rooms.yaml"
      provides: "Complete room data for all 18 room types"
      contains: "latrine"
    - path: "src/content.config.ts"
      provides: "Schema definitions for rooms + per-family critter collections"
      exports: ["collections"]
    - path: "src/components/RoomEntry.astro"
      provides: "Room card component with dimensions, buildings, bonus, upgrade path"
    - path: "src/pages/base-layouts/index.astro"
      provides: "Rooms reference page rendering all 18 rooms"
  key_links:
    - from: "src/pages/base-layouts/index.astro"
      to: "src/data/rooms.yaml"
      via: "getCollection('rooms')"
      pattern: "getCollection.*rooms"
    - from: "src/content.config.ts"
      to: "src/data/rooms.yaml"
      via: "file() loader"
      pattern: "file.*rooms\\.yaml"
---

<objective>
Create the data layer for all Phase 2 content and build the complete rooms reference page.

Purpose: Establishes the content.config schema for rooms and per-family critter collections (used by Plans 02 and 03), and delivers the full rooms reference page with all 18 standard room types.
Output: Rooms YAML data, updated content.config.ts with all new collections, RoomEntry component, and the rooms page replacing the base-layouts stub.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/content.config.ts
@src/data/hatches.yaml
@src/components/StatPill.astro
@src/layouts/BaseLayout.astro
@src/pages/base-layouts/index.astro

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/content.config.ts:
```typescript
const unitValue = z.object({ value: z.number(), unit: z.string() });
// Current: single 'critters' collection from file('src/data/hatches.yaml')
export const collections = { critters };
```

From src/components/StatPill.astro:
```typescript
interface Props { value: string | number; unit?: string; }
```

From src/layouts/BaseLayout.astro:
```typescript
interface Props { title: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rooms data, update content.config with all Phase 2 collections, and update hatches.yaml schema</name>
  <files>src/data/rooms.yaml, src/content.config.ts, src/data/hatches.yaml</files>
  <action>
1. Create `src/data/rooms.yaml` with ALL 18 standard room types from the game. Each entry has these fields:
   - id (slug), name, dimensions (min/max tile counts), required_buildings (array of building names), bonus (string describing morale or effect), upgrade_from (nullable id), upgrade_to (nullable id), layout_tip (string with practical advice), diagram_slot (boolean, true for all), dlc (array, typically ["base"])

   Complete room list (use the research data in 02-RESEARCH.md "Complete Room Types" table for exact values):
   Latrine, Washroom, Barracks, Luxury Barracks, Private Bedroom, Mess Hall, Great Hall, Banquet Hall, Kitchen, Massage Clinic, Hospital, Power Plant, Greenhouse, Stable, Laboratory, Recreation Room, Park, Nature Reserve

   Upgrade chains to encode: Latrine->Washroom, Barracks->Luxury Barracks->Private Bedroom, Mess Hall->Great Hall->Banquet Hall, Park->Nature Reserve

2. Update `src/content.config.ts`:
   - Keep the existing `unitValue` helper
   - Create a `critterSchema` with extended fields: id, name, family (string), dlc, diet, output (product + rate + optional method string for "excretion"/"shearing"/"ambient"/"on_death"), calories.metabolism, stable_capacity, space_per_critter (optional number), temperature (min/max unitValue), grooming (station + cycle_time), image_slot (boolean default true), notes (optional string)
   - Create a `roomSchema` with fields: id, name, dimensions (min/max numbers), required_buildings (array of strings), bonus (string), upgrade_from (nullable string), upgrade_to (nullable string), layout_tip (string), diagram_slot (boolean default true), dlc (array of strings)
   - Register separate collections per critter family: hatches, dreckos, pacus, pips, slicksters, shineBugs -- each using `file()` loader pointing to respective YAML file. The YAML files for dreckos/pacus/pips/slicksters/shine-bugs don't exist yet -- that's fine, they won't be imported until Plan 03 creates them. Just define the collections.
   - Register rooms collection using `file('src/data/rooms.yaml')`
   - Export all collections
   - IMPORTANT: Remove the old single `critters` collection. Replace with `hatches` collection using file('src/data/hatches.yaml')

3. Update `src/data/hatches.yaml` to include the new schema fields:
   - Add `family: Hatch` to each entry
   - Add `image_slot: true` to each entry
   - Add `output.method: excretion` to each entry
   - Keep all existing data intact

NOTE: After changing the collection name from `critters` to `hatches`, the build will temporarily fail because hatches.astro still imports `getCollection('critters')`. That's expected -- Plan 02 will update hatches.astro.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && node -e "const fs=require('fs'); const yaml=require('js-yaml'); const d=yaml.load(fs.readFileSync('src/data/rooms.yaml','utf8')); console.log('Rooms:',d.length); if(d.length!==18) throw new Error('Expected 18 rooms, got '+d.length); console.log('OK')" 2>/dev/null || echo "Verify rooms.yaml has 18 entries manually"</automated>
  </verify>
  <done>rooms.yaml has all 18 room types with complete data. content.config.ts defines roomSchema, extended critterSchema, and registers collections for rooms + 6 critter families. hatches.yaml has new family/image_slot/method fields.</done>
</task>

<task type="auto">
  <name>Task 2: Build RoomEntry component and rooms reference page</name>
  <files>src/components/RoomEntry.astro, src/pages/base-layouts/index.astro</files>
  <action>
1. Create `src/components/RoomEntry.astro`:
   - Props: room object with data matching roomSchema
   - Display: room name as heading, bonus as a teal badge (top-right), dimensions as StatPill range ("12 tiles to 64 tiles"), required buildings as tag chips, layout tip as italic text, diagram placeholder slot (aspect-video bg-oni-bg-elevated rounded-lg with centered placeholder text)
   - Upgrade path indicator: if upgrade_from or upgrade_to is set, show a small badge/link text like "Upgrades to: Washroom" or "Upgraded from: Latrine" using oni-text-muted styling
   - Add an `id` attribute on the container div using the room's id slug so users can link directly (e.g., #washroom)
   - Use existing Tailwind tokens: bg-oni-bg-surface, border-oni-bg-elevated, text-oni-text-primary, text-oni-text-secondary, text-oni-text-muted, text-oni-teal
   - Mobile responsive: single column stacking

2. Replace `src/pages/base-layouts/index.astro` stub with full rooms page:
   - Import BaseLayout, RoomEntry, getCollection
   - Fetch rooms collection via `await getCollection('rooms')`
   - Page heading: "Rooms Reference" with description "Every standard room type the game recognizes, with dimensions, required buildings, bonuses, and layout tips."
   - Group rooms by upgrade chains: show upgrade chain groups first (Latrine chain, Barracks chain, Mess Hall chain, Park chain), then standalone rooms. Use a simple approach: render all rooms in a logical order, with upgrade chain rooms adjacent to each other.
   - Render each room using RoomEntry component
   - Breadcrumb: Base Layouts > Rooms (but since it's the index, just show the heading)
   - Keep the /oni-fanpage/ base path in any internal links
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npx astro check 2>&1 | tail -5; echo "---"; npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>Rooms page at /base-layouts/ renders all 18 room types with dimensions, required buildings, bonuses, upgrade paths, layout tips, and diagram placeholders. Build succeeds (note: hatches page may fail due to collection rename -- that is expected and fixed in Plan 02).</done>
</task>

</tasks>

<verification>
- rooms.yaml contains exactly 18 room entries with all required fields
- content.config.ts exports collections for rooms + 6 critter families
- RoomEntry component renders room data with all fields visible
- Rooms page replaces the stub at /base-layouts/
- All upgrade chains are correctly encoded (upgrade_from/upgrade_to match)
</verification>

<success_criteria>
User can visit the base-layouts page and see all 18 standard room types with their dimensions, required buildings, bonuses, upgrade paths, and layout tips. The data layer is prepared for Plans 02 and 03 to add critter content.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rooms-ranching/02-01-SUMMARY.md`
</output>
