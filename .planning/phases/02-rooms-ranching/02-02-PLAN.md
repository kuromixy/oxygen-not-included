---
phase: 02-rooms-ranching
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/CritterVariantCard.astro
  - src/components/RanchBuildTabs.astro
  - src/components/CritterCalculator.astro
  - src/pages/ranching/hatches.astro
autonomous: true
requirements:
  - RNCH-01
  - RNCH-02
  - RNCH-03
  - RNCH-04

must_haves:
  truths:
    - "User can see ranch build layout with tab views (Overview, Automation, Power, Plumbing) at top of critter page"
    - "User can see each critter variant in its own card with diet, output, capacity, grooming stats"
    - "User can input a critter count and see calculated total input/output per cycle"
    - "Calculator defaults to max stable capacity for the critter family"
    - "Variant differences are clearly visible on the same page"
  artifacts:
    - path: "src/components/RanchBuildTabs.astro"
      provides: "Tab switcher with 4 ranch build views and placeholder image slots"
    - path: "src/components/CritterVariantCard.astro"
      provides: "Per-variant card with image slot, diet, output, capacity, grooming, temperature"
    - path: "src/components/CritterCalculator.astro"
      provides: "Interactive calculator with critter count input and calculated resource rates"
    - path: "src/pages/ranching/hatches.astro"
      provides: "Revised Hatches page using new template (RanchBuildTabs + VariantCards + Calculator)"
  key_links:
    - from: "src/pages/ranching/hatches.astro"
      to: "src/components/RanchBuildTabs.astro"
      via: "component import"
      pattern: "import RanchBuildTabs"
    - from: "src/pages/ranching/hatches.astro"
      to: "src/components/CritterCalculator.astro"
      via: "component import"
      pattern: "import CritterCalculator"
    - from: "src/pages/ranching/hatches.astro"
      to: "getCollection('hatches')"
      via: "content collection fetch"
      pattern: "getCollection.*hatches"
---

<objective>
Create the critter page template components and revise the Hatches page to use the new layout.

Purpose: Establishes the reusable critter page template (RanchBuildTabs + CritterVariantCard + CritterCalculator) that all 6 critter family pages will follow. The Hatches page is revised first to validate the template before replicating it in Plan 03.
Output: Three new components and an updated Hatches page demonstrating the complete critter page template.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rooms-ranching/02-01-SUMMARY.md

@src/components/StatPill.astro
@src/components/DetailCard.astro
@src/layouts/BaseLayout.astro
@src/data/hatches.yaml

<interfaces>
<!-- From Plan 01 output: content.config.ts critterSchema -->
<!-- The critter schema has these fields (per-variant): -->
<!-- id, name, family, dlc, diet, output (product + rate + method?), calories.metabolism, stable_capacity, space_per_critter?, temperature (min/max), grooming (station + cycle_time), image_slot, notes? -->

From src/components/StatPill.astro:
```typescript
interface Props { value: string | number; unit?: string; }
```

From src/layouts/BaseLayout.astro:
```typescript
interface Props { title: string; }
```

<!-- Collection name changed from 'critters' to 'hatches' in Plan 01 -->
<!-- Use: const variants = await getCollection('hatches'); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RanchBuildTabs and CritterVariantCard components</name>
  <files>src/components/RanchBuildTabs.astro, src/components/CritterVariantCard.astro</files>
  <action>
1. Create `src/components/RanchBuildTabs.astro`:
   - Props: `family` (string) -- used to generate unique radio input names
   - Renders 4 tabs: Overview, Automation, Power, Plumbing
   - Implementation: Use CSS-only radio button approach OR minimal JS (Claude's discretion per CONTEXT.md). If CSS-only, use hidden radio inputs + labels + sibling-selected panels in a flat container. If JS, use a small inline script with click listeners toggling a CSS class.
   - Each tab panel contains a placeholder image slot: aspect-video container with bg-oni-bg-elevated, rounded-lg, centered text "[Tab name] diagram -- coming soon"
   - Tab labels should have: inactive state (text-oni-text-muted, bg-oni-bg-surface), active state (text-oni-teal, border-bottom or bg highlight), cursor-pointer, rounded-t-lg
   - Container has border border-oni-bg-elevated rounded-xl overflow-hidden
   - Section heading above tabs: "Ranch Build Layout" in text-lg font-bold

2. Create `src/components/CritterVariantCard.astro`:
   - Props: `entry` object matching the critterSchema from content.config.ts
   - Layout: Card with border border-oni-bg-elevated rounded-xl p-5
   - Top section: Variant name (h3, text-xl font-bold), per-variant placeholder image slot (w-20 h-20 rounded-lg bg-oni-bg-elevated with centered critter emoji placeholder) displayed side by side
   - Stats grid (grid-cols-1 md:grid-cols-2 gap-4):
     - Diet: tag chips for each diet item (format slug to title case: split on _ , capitalize each word)
     - Output: product name + rate as StatPill. If method field exists and is not "excretion", show method in parentheses (e.g., "Reed Fiber at 0.25 kg/cycle (shearing)")
     - Metabolism: StatPill with value + unit
     - Temperature Range: min StatPill "to" max StatPill
     - Stable Capacity: StatPill with unit "critters"
     - Grooming: station name (formatted) + cycle time StatPill
     - DLC: tag chips
   - Notes section: if notes field exists, show in a subtle bg-oni-bg-elevated/50 box with italic text
   - Use the same `formatName` helper function as DetailCard (split on _, capitalize each word)
   - This component does NOT use details/summary -- it's always expanded (per user decision to remove ComparisonTable and show variants directly)
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>RanchBuildTabs renders 4 tabbed panels with placeholder image slots. CritterVariantCard renders all variant stats in a card layout with image placeholder, diet chips, output with method, and all stats using StatPill.</done>
</task>

<task type="auto">
  <name>Task 2: Create CritterCalculator and revise Hatches page</name>
  <files>src/components/CritterCalculator.astro, src/pages/ranching/hatches.astro</files>
  <action>
1. Create `src/components/CritterCalculator.astro`:
   - Props: `variants` array (the collection entries), `defaultCapacity` (number -- the family's stable capacity, passed from the page)
   - Renders a section with heading "Resource Calculator"
   - Contains: a dropdown/select to pick a variant, a number input for critter count (min=1, max=50, defaults to `defaultCapacity`), and a results display area
   - Use a `<script>` tag (inline, vanilla JS) for interactivity:
     - Serialize variant data to a `data-variants` attribute on the container as JSON (map to: name, output.product, output.rate.value, output.rate.unit, stable_capacity)
     - On page load, read data-variants, set up event listeners on select and input
     - When variant or count changes, calculate: total output = count * rate.value, display as "[count] x [variant] = [total] [unit] of [product] per cycle"
     - On initial load, select first variant and use defaultCapacity as count
   - Style: bg-oni-bg-surface border border-oni-bg-elevated rounded-xl p-5. Input uses bg-oni-bg-elevated text-oni-text-primary border-oni-bg-elevated rounded px-3 py-2. Select uses same styling. Results in text-lg text-oni-teal font-mono.

2. Rewrite `src/pages/ranching/hatches.astro` with new layout:
   - Import BaseLayout, RanchBuildTabs, CritterVariantCard, CritterCalculator, getCollection
   - Fetch: `const variants = await getCollection('hatches');` (NOT 'critters' -- collection was renamed in Plan 01)
   - Set familyName = 'Hatches', familyDescription = 'The Hatch family eats minerals and produces coal or refined metals.'
   - Page structure (per user decision):
     a. Breadcrumb: Ranching > Hatches (same pattern as before)
     b. Page heading: h1 "Hatches" + description paragraph
     c. Section: Ranch Build Layout -- `<RanchBuildTabs family="hatches" />`
     d. Section: Variants -- heading "Variants" + map variants to CritterVariantCard components
     e. Section: Calculator -- `<CritterCalculator variants={variants} defaultCapacity={8} />`
   - Remove the ComparisonTable import and usage (per locked user decision)
   - Keep the /oni-fanpage/ base path in breadcrumb links
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>Hatches page shows Ranch Build Layout tabs at top, variant cards with all stats below, and a working calculator at the bottom. ComparisonTable is removed. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- Hatches page has new layout: RanchBuildTabs -> VariantCards -> Calculator
- ComparisonTable is NOT used on the Hatches page
- Tab switcher shows 4 views with placeholder image slots
- Each variant has its own card with all stats displayed
- Calculator defaults to 8 critters (Hatch stable capacity)
- Calculator updates results when count or variant changes
- Build succeeds with `npm run build`
</verification>

<success_criteria>
User can visit the Hatches page and see the ranch build tab layout at the top, individual variant cards with diet/output/capacity/grooming stats below, and a working resource calculator at the bottom that defaults to 8 critters and shows per-cycle output.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rooms-ranching/02-02-SUMMARY.md`
</output>
