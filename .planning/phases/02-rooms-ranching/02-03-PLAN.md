---
phase: 02-rooms-ranching
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/data/dreckos.yaml
  - src/data/pacus.yaml
  - src/data/pips.yaml
  - src/data/slicksters.yaml
  - src/data/shine-bugs.yaml
  - src/pages/ranching/dreckos.astro
  - src/pages/ranching/pacus.astro
  - src/pages/ranching/pips.astro
  - src/pages/ranching/slicksters.astro
  - src/pages/ranching/shine-bugs.astro
  - src/pages/ranching/index.astro
autonomous: true
requirements:
  - RNCH-01
  - RNCH-02
  - RNCH-03
  - RNCH-04

must_haves:
  truths:
    - "User can look up any of 6 critter families and see diet, output, and resource rates"
    - "User can see stable capacity for every critter variant"
    - "User can see grooming station requirements for each critter"
    - "User can see all variants within each family with differences highlighted"
    - "Ranching hub links to all 6 critter family pages (no more Coming soon badges)"
  artifacts:
    - path: "src/data/dreckos.yaml"
      provides: "Drecko and Glossy Drecko variant data"
      contains: "drecko"
    - path: "src/data/pacus.yaml"
      provides: "Pacu, Tropical Pacu, Gulp Fish variant data"
      contains: "pacu"
    - path: "src/data/pips.yaml"
      provides: "Pip and Cuddle Pip variant data"
      contains: "pip"
    - path: "src/data/slicksters.yaml"
      provides: "Slickster, Molten, Longhair variant data"
      contains: "slickster"
    - path: "src/data/shine-bugs.yaml"
      provides: "All 7 Shine Bug variant data"
      contains: "shine_bug"
    - path: "src/pages/ranching/index.astro"
      provides: "Updated ranching hub with active links to all 6 families"
  key_links:
    - from: "src/pages/ranching/dreckos.astro"
      to: "getCollection('dreckos')"
      via: "content collection fetch"
      pattern: "getCollection.*dreckos"
    - from: "src/pages/ranching/index.astro"
      to: "all critter pages"
      via: "href links"
      pattern: "/oni-fanpage/ranching/"
---

<objective>
Create data files and pages for all 5 remaining critter families and activate the ranching hub links.

Purpose: Completes the ranching content domain. All 6 critter families (Hatches + 5 new) are viewable, and the ranching hub is fully linked with no placeholder entries.
Output: 5 YAML data files, 5 critter pages, updated ranching hub.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rooms-ranching/02-02-SUMMARY.md

@src/data/hatches.yaml
@src/pages/ranching/hatches.astro
@src/pages/ranching/index.astro

<interfaces>
<!-- From Plan 02: the critter page template pattern -->
<!-- Each page follows: import BaseLayout, RanchBuildTabs, CritterVariantCard, CritterCalculator -->
<!-- Fetch: const variants = await getCollection('{familyCollection}'); -->
<!-- Layout: Breadcrumb -> Heading -> RanchBuildTabs -> Variant Cards -> Calculator -->

<!-- Collection names registered in Plan 01 content.config.ts: -->
<!-- hatches, dreckos, pacus, pips, slicksters, shineBugs -->

<!-- YAML schema fields per variant: -->
<!-- id, name, family, dlc, diet, output (product + rate + method?), calories.metabolism, stable_capacity, space_per_critter?, temperature (min/max), grooming (station + cycle_time), image_slot, notes? -->

From src/components/CritterVariantCard.astro:
```typescript
// Props: entry object matching critterSchema
```

From src/components/RanchBuildTabs.astro:
```typescript
// Props: { family: string }
```

From src/components/CritterCalculator.astro:
```typescript
// Props: { variants: CollectionEntry[], defaultCapacity: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML data files for all 5 critter families</name>
  <files>src/data/dreckos.yaml, src/data/pacus.yaml, src/data/pips.yaml, src/data/slicksters.yaml, src/data/shine-bugs.yaml</files>
  <action>
Create 5 YAML data files following the exact same schema as hatches.yaml (with the new fields from Plan 01: family, image_slot, output.method).

Use the research data from 02-RESEARCH.md "Critter Families Summary" and "Special cases" sections. Key data per family:

**dreckos.yaml** (2 variants):
- Drecko: family=Drecko, diet=[balm_lily, pincha_pepperplant, mealwood], output=reed_fiber at ~0.25 kg/cycle method=shearing, stable_capacity=8, temp -30 to 100C, grooming_station 1 cycle. Notes about hydrogen atmosphere for scale growth.
- Glossy Drecko: diet=[mealwood, bristle_blossom], output=plastic at ~50 g/cycle method=shearing, stable_capacity=8. Notes about hydrogen atmosphere.

**pacus.yaml** (3 variants):
- Pacu: family=Pacu, diet=[algae, seeds], output=pacu_fillet at ~1 kg/cycle method=on_death, stable_capacity=8, grooming station=fish_feeder 1 cycle. Notes: Lives in water. Main value is meat on death. Also excretes polluted dirt.
- Tropical Pacu: diet=[algae, seeds], output=pacu_fillet method=on_death, stable_capacity=8. Notes about warm water requirement.
- Gulp Fish: diet=[polluted_water], output=clean_water method=excretion, stable_capacity=8. Notes about water purification.

**pips.yaml** (2 variants):
- Pip: family=Pip, diet=[raw_minerals, seeds], output=dirt at ~20 kg/cycle method=excretion, stable_capacity=8, temp -30 to 70C, grooming_station 1 cycle. Notes about wild planting ability.
- Cuddle Pip: diet=[seeds], output=dirt method=excretion, stable_capacity=24, space_per_critter=4. Notes about small size, high density.

**slicksters.yaml** (3 variants):
- Slickster: family=Slickster, diet=[carbon_dioxide], output=crude_oil at ~20 kg/cycle method=excretion, stable_capacity=8, temp 35 to 80C, grooming_station 1 cycle.
- Molten Slickster: diet=[carbon_dioxide], output=petroleum method=excretion, stable_capacity=8, temp 110 to 160C. Notes about extreme heat requirement.
- Longhair Slickster: diet=[oxygen], output=none at 0 method=ambient, stable_capacity=8, temp -30 to 60C. Notes: produces decor only, no material output.

**shine-bugs.yaml** (7 variants):
- Shine Bug: family=Shine Bug, diet=[phosphorite], output=light at ~1800 lux method=ambient, stable_capacity=16, space_per_critter=6, temp -30 to 70C, grooming_station 1 cycle. Notes about decor/light output.
- Sun Bug: diet=[phosphorite], output=light method=ambient, stable_capacity=16. DLC: spaced_out.
- Royal Bug: diet=[phosphorite], output=light method=ambient, stable_capacity=16.
- Coral Bug: diet=[phosphorite], output=light method=ambient, stable_capacity=16.
- Azure Bug: diet=[phosphorite], output=light method=ambient, stable_capacity=16.
- Abyss Bug: diet=[phosphorite], output=light method=ambient, stable_capacity=16. Notes about negative light/darkness.
- Radiant Bug: diet=[phosphorite], output=light method=ambient, stable_capacity=16.

For each variant, include: id (slug format), name, family, dlc (["base"] unless Spaced Out specific), all required schema fields. Use approximate rates from research -- these are for quick reference, not simulation accuracy.

IMPORTANT: stable_capacity varies per critter type. Do NOT hardcode 8 for all. Shine Bugs=16, Cuddle Pip=24.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && node -e "const fs=require('fs');const y=require('js-yaml');['dreckos','pacus','pips','slicksters','shine-bugs'].forEach(f=>{const d=y.load(fs.readFileSync('src/data/'+f+'.yaml','utf8'));console.log(f+': '+d.length+' variants')})"</automated>
  </verify>
  <done>All 5 YAML files exist with correct variant counts: dreckos (2), pacus (3), pips (2), slicksters (3), shine-bugs (7). Each variant has all required schema fields including family, image_slot, output.method, and correct stable_capacity.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 critter pages and activate ranching hub links</name>
  <files>src/pages/ranching/dreckos.astro, src/pages/ranching/pacus.astro, src/pages/ranching/pips.astro, src/pages/ranching/slicksters.astro, src/pages/ranching/shine-bugs.astro, src/pages/ranching/index.astro</files>
  <action>
1. Create 5 critter page files, each following the EXACT same template pattern as the revised hatches.astro from Plan 02:

   Each page structure:
   ```
   ---
   import BaseLayout from '../../layouts/BaseLayout.astro';
   import RanchBuildTabs from '../../components/RanchBuildTabs.astro';
   import CritterVariantCard from '../../components/CritterVariantCard.astro';
   import CritterCalculator from '../../components/CritterCalculator.astro';
   import { getCollection } from 'astro:content';

   const variants = await getCollection('{collectionName}');
   const familyName = '{FamilyName}';
   const familyDescription = '{description}';
   ---
   <BaseLayout title="{FamilyName} -- ONI Reference">
     Breadcrumb -> Heading -> RanchBuildTabs -> Variant Cards -> Calculator
   </BaseLayout>
   ```

   Per-page specifics:
   - **dreckos.astro**: collection='dreckos', family='Dreckos', desc='The Drecko family produces fibers and plastic via shearing. Requires hydrogen atmosphere for scale growth.', defaultCapacity=8
   - **pacus.astro**: collection='pacus', family='Pacus', desc='Pacus are aquatic critters. Their main value is Pacu Fillet (on death) and water-related outputs.', defaultCapacity=8
   - **pips.astro**: collection='pips', family='Pips', desc='Pips produce dirt and can plant seeds in wild conditions. Cuddle Pips are compact and high-density.', defaultCapacity=8
   - **slicksters.astro**: collection='slicksters', family='Slicksters', desc='Slicksters consume gases and produce petroleum products. Temperature requirements vary widely by variant.', defaultCapacity=8
   - **shine-bugs.astro**: collection='shineBugs', family='Shine Bugs', desc='Shine Bugs produce light and decor. High stable capacity (16) with many color variants.', defaultCapacity=16

   IMPORTANT: Use the correct collection names as registered in content.config.ts from Plan 01. The collection name for shine-bugs is 'shineBugs' (camelCase in JS).

   Use /oni-fanpage/ base path in all breadcrumb links.

2. Update `src/pages/ranching/index.astro`:
   - Change ALL critter entries to active with working hrefs:
     - Hatches: /oni-fanpage/ranching/hatches/
     - Dreckos: /oni-fanpage/ranching/dreckos/
     - Pacus: /oni-fanpage/ranching/pacus/
     - Pips: /oni-fanpage/ranching/pips/
     - Slicksters: /oni-fanpage/ranching/slicksters/
     - Shine Bugs: /oni-fanpage/ranching/shine-bugs/
   - Remove the "Coming soon" badge and opacity-50 styling from all entries
   - All entries should use the active link pattern (the <a> tag version, not the <div> placeholder)
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npx astro build 2>&1 | tail -15</automated>
  </verify>
  <done>All 6 critter family pages build successfully. Ranching hub links to all 6 families with no placeholder entries. Each page shows ranch build tabs, variant cards with per-variant stats, and a working calculator. Full build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- All 6 critter pages are generated in the build output
- Ranching hub has 6 active links, no "Coming soon" badges
- Each critter page shows: ranch build tabs, variant cards, calculator
- Stable capacity defaults are correct: 8 for most, 16 for Shine Bugs, 24 for Cuddle Pip
- Special mechanics noted: Drecko shearing, Pacu on_death, Longhair Slickster decor-only
</verification>

<success_criteria>
User can navigate from the ranching hub to any of the 6 critter family pages and see the complete ranch reference: build layout tabs, variant cards with all stats (diet, output, capacity, grooming, temperature), and a working resource calculator. No placeholder "Coming soon" entries remain on the ranching hub.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rooms-ranching/02-03-SUMMARY.md`
</output>
