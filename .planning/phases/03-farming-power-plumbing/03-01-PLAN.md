---
phase: 03-farming-power-plumbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/data/crops.yaml
  - src/components/CropComparisonTable.astro
  - src/components/CropStatCard.astro
  - src/pages/farming/index.astro
  - src/pages/farming/mealwood.astro
  - src/pages/farming/bristle-blossom.astro
  - src/pages/farming/sleet-wheat.astro
  - src/pages/farming/dusk-cap.astro
  - src/pages/farming/pincha-pepper.astro
  - src/pages/farming/waterweed.astro
  - src/pages/farming/nosh-sprout.astro
autonomous: true
requirements:
  - FARM-01
  - FARM-02
  - FARM-03

must_haves:
  truths:
    - "User can visit farming hub and see all 7 crops in a comparison table with growth time, kcal/cycle, irrigation, temp range, and fertilizer columns"
    - "User can click any crop row in the comparison table and navigate to that crop's detail page"
    - "User can view any individual crop page and see temperature range, irrigation needs, fertilizer, growth time, and calorie output"
    - "Each crop page follows critter page pattern: breadcrumb, heading, RanchBuildTabs, stat cards"
  artifacts:
    - path: "src/data/crops.yaml"
      provides: "All 7 crop data entries with verified game stats"
      contains: "mealwood"
    - path: "src/components/CropComparisonTable.astro"
      provides: "Side-by-side crop comparison on farming hub"
      min_lines: 30
    - path: "src/components/CropStatCard.astro"
      provides: "Stat card for individual crop pages"
      min_lines: 20
    - path: "src/pages/farming/index.astro"
      provides: "Farming hub with comparison table"
      contains: "CropComparisonTable"
    - path: "src/content.config.ts"
      provides: "crops collection with cropSchema"
      contains: "cropSchema"
  key_links:
    - from: "src/pages/farming/index.astro"
      to: "src/data/crops.yaml"
      via: "getCollection('crops')"
      pattern: "getCollection.*crops"
    - from: "src/components/CropComparisonTable.astro"
      to: "/oni-fanpage/farming/{crop-id}/"
      via: "href links in table rows"
      pattern: "oni-fanpage/farming/"
    - from: "src/pages/farming/mealwood.astro"
      to: "src/components/CropStatCard.astro"
      via: "component import"
      pattern: "CropStatCard"
---

<objective>
Farming domain: crop data layer, comparison table, hub page, and 7 individual crop pages.

Purpose: Satisfy FARM-01/02/03 -- users can look up any crop's stats and compare crops side-by-side on the farming hub.
Output: Working farming section with data-driven crop pages following established critter page patterns.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-farming-power-plumbing/03-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/content.config.ts:
```typescript
import { defineCollection } from 'astro:content';
import { file } from 'astro/loaders';
import { z } from 'astro/zod';

const unitValue = z.object({
  value: z.number(),
  unit: z.string(),
});

// Existing collections: hatches, dreckos, pacus, pips, slicksters, shineBugs, rooms
export const collections = { hatches, dreckos, pacus, pips, slicksters, shineBugs, rooms };
```

From src/components/StatPill.astro (reuse for stat display):
- Accepts `value` and `unit` props
- Renders highlighted numeric pill with oni-stat-pill-bg/text tokens

From src/components/RanchBuildTabs.astro:
- Accepts `family: string` prop (used to namespace radio inputs)
- Renders 4 tabs: Overview, Automation, Power, Plumbing with placeholder image slots
- Inline JS for tab switching

From src/pages/ranching/hatches.astro (page template pattern):
```
Breadcrumb -> Heading -> RanchBuildTabs(family="hatches") -> Variant cards -> Calculator
```

From src/components/CritterVariantCard.astro (layout pattern to adapt):
- Card with icon placeholder + name at top
- 2-column stats grid below (using StatPill)
- Notes section at bottom
- formatName helper for slug display
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crop data layer -- schema, YAML, and content collection</name>
  <files>src/content.config.ts, src/data/crops.yaml</files>
  <action>
1. Add cropSchema to content.config.ts after existing schemas. Use the existing `unitValue` z.object. Schema fields:
   - id: z.string()
   - name: z.string()
   - dlc: z.array(z.string())
   - growth_cycles: z.number() -- use 7.5 for Dusk Cap (exact value, not rounded)
   - kcal_per_harvest: z.number()
   - kcal_per_cycle: z.number() -- pre-computed, stored explicitly
   - temperature: z.object({ min: unitValue, max: unitValue })
   - irrigation: z.object({ liquid: z.string().nullable(), liquid_amount: unitValue.nullable(), solid: z.string().nullable(), solid_amount: unitValue.nullable() })
   - fertilizer: z.string().nullable()
   - light_required: z.number().nullable() -- null=none, 0=darkness, 200+=lux requirement
   - notes: z.string().optional()
   - image_slot: z.boolean().default(true)

2. Define crops collection: `const crops = defineCollection({ loader: file('src/data/crops.yaml'), schema: cropSchema });`

3. Add crops to the exports: `export const collections = { ...existing, crops };`

4. Create src/data/crops.yaml with all 7 crops. Use exact data from 03-RESEARCH.md "Crop YAML Data" section. Include all 7: Mealwood, Bristle Blossom, Sleet Wheat, Dusk Cap (growth_cycles: 7.5, kcal_per_cycle: 613), Pincha Pepper, Waterweed (dlc: spaced_out), Nosh Sprout (dlc: spaced_out).

IMPORTANT: Guard nullable fields in YAML -- use `null` explicitly for fields like irrigation.liquid when crop has no liquid irrigation (e.g., Mealwood). Do NOT omit nullable fields.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>crops.yaml has 7 entries, cropSchema validates all entries, build succeeds with no collection errors</done>
</task>

<task type="auto">
  <name>Task 2: Farming hub with comparison table and crop stat card component</name>
  <files>src/components/CropComparisonTable.astro, src/components/CropStatCard.astro, src/pages/farming/index.astro</files>
  <action>
1. Create CropComparisonTable.astro -- adapts the ComparisonTable desktop-grid/mobile-stacked pattern:
   - Props: `crops` array (from getCollection)
   - Desktop view (hidden on mobile, md:block): CSS grid with columns: Crop Name, Growth (cycles), kcal/cycle, Irrigation, Temp Range, Fertilizer
   - Header row with text-oni-text-muted uppercase labels
   - Data rows: each crop name is an anchor link to `/oni-fanpage/farming/{crop.data.id}/` (replace underscores with hyphens in the href slug: bristle_blossom -> bristle-blossom)
   - Use StatPill for numeric values (growth_cycles, kcal_per_cycle, temperature min/max)
   - For irrigation column: show "{liquid_amount} {liquid}" and/or "{solid_amount} {solid}" -- guard nulls (don't render "null")
   - For fertilizer column: show fertilizer name or "--" if null
   - Row hover: hover:bg-oni-bg-elevated/50 transition
   - Mobile view (md:hidden): stacked cards with crop name as heading, stats listed vertically, entire card tappable linking to crop page
   - Static order (no JS sorting): Mealwood, Bristle Blossom, Dusk Cap, Pincha Pepper, Sleet Wheat, Waterweed, Nosh Sprout

2. Create CropStatCard.astro -- adapts CritterVariantCard layout for crop data:
   - Props: `entry` (single crop from collection)
   - Card with border border-oni-bg-elevated rounded-xl p-5
   - Top section: plant emoji placeholder (use a leaf or seedling emoji) + crop name
   - 2-column stats grid (grid-cols-1 md:grid-cols-2 gap-4):
     * Growth Time: StatPill with growth_cycles + "cycles"
     * Harvest Yield: StatPill with kcal_per_harvest + "kcal" and kcal_per_cycle + "kcal/cycle"
     * Temperature Range: min to max with StatPill (same pattern as CritterVariantCard)
     * Irrigation: show liquid and/or solid requirements, guard nulls
     * Fertilizer: show if present, "--" or "None" if null
     * Light: show lux requirement if light_required is not null. "Darkness required" if 0. Omit section if null.
     * DLC Compatibility: pill badges (same as CritterVariantCard)
   - Notes section at bottom (same pattern as CritterVariantCard)

3. Rewrite src/pages/farming/index.astro (currently a stub):
   - Import BaseLayout, CropComparisonTable, getCollection
   - Fetch crops: `const crops = await getCollection('crops');`
   - Page heading: "Farming" with subtitle "Crop temperatures, irrigation, growth times, and calorie yields."
   - Render CropComparisonTable passing crops
   - Below the table, add a grid of CategoryCard-style links to individual crop pages (grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4), each linking to `/oni-fanpage/farming/{id}/` with the crop name. Use the same card style as ranching hub (bg-oni-bg-surface border border-oni-bg-elevated rounded-lg p-4 hover:border-oni-teal).
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Farming hub renders comparison table with all 7 crops, each row links to crop detail page, mobile view shows stacked cards</done>
</task>

<task type="auto">
  <name>Task 3: Individual crop pages (7 pages following critter page template)</name>
  <files>src/pages/farming/mealwood.astro, src/pages/farming/bristle-blossom.astro, src/pages/farming/sleet-wheat.astro, src/pages/farming/dusk-cap.astro, src/pages/farming/pincha-pepper.astro, src/pages/farming/waterweed.astro, src/pages/farming/nosh-sprout.astro</files>
  <action>
Create 7 crop pages. Each follows the hatches.astro pattern exactly:

1. Frontmatter:
   - Import BaseLayout, RanchBuildTabs, CropStatCard, getCollection
   - Fetch all crops: `const allCrops = await getCollection('crops');`
   - Filter to the specific crop: `const crop = allCrops.find(c => c.data.id === '{crop_id}');`
   - Use the YAML id values: mealwood, bristle_blossom, sleet_wheat, dusk_cap, pincha_pepper, waterweed, nosh_sprout

2. Page structure (matching critter page template):
   - Breadcrumb: Farming > {Crop Name} (link Farming to /oni-fanpage/farming/)
   - Page heading: h1 with crop name, subtitle with brief description from crop notes
   - RanchBuildTabs with family="{crop-slug}" (use the page filename slug, e.g., "mealwood", "bristle-blossom")
   - CropStatCard rendering the crop entry
   - No calculator needed for crops

3. File name to YAML id mapping (filenames use hyphens, YAML ids use underscores):
   - mealwood.astro -> id: mealwood
   - bristle-blossom.astro -> id: bristle_blossom
   - sleet-wheat.astro -> id: sleet_wheat
   - dusk-cap.astro -> id: dusk_cap
   - pincha-pepper.astro -> id: pincha_pepper
   - waterweed.astro -> id: waterweed
   - nosh-sprout.astro -> id: nosh_sprout

4. Page title: "{Crop Name} -- ONI Reference"

5. All internal links use /oni-fanpage/ base path prefix.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 crop pages build successfully, each shows breadcrumb + heading + build tabs + stat card, navigable from farming hub</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- Farming hub at /farming/ shows comparison table with 7 crops
- Each crop row in comparison table links to individual crop page
- Each crop page shows temperature range, irrigation, fertilizer, growth time, calorie output
- Nullable fields (e.g., Mealwood liquid irrigation) render cleanly without "null" text
</verification>

<success_criteria>
- 7 crop data entries in crops.yaml validated by Zod schema
- CropComparisonTable renders desktop grid and mobile cards
- Farming hub replaces placeholder with live comparison table + crop links
- All 7 crop detail pages follow critter page template (breadcrumb, tabs, stat card)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-farming-power-plumbing/03-01-SUMMARY.md`
</output>
