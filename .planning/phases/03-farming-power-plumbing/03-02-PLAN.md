---
phase: 03-farming-power-plumbing
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/content.config.ts
  - src/data/generators.yaml
  - src/data/batteries.yaml
  - src/data/geysers.yaml
  - src/components/BuildStatCard.astro
  - src/pages/power-plumbing/index.astro
  - src/pages/power-plumbing/coal-generator.astro
  - src/pages/power-plumbing/natural-gas-generator.astro
  - src/pages/power-plumbing/hydrogen-generator.astro
  - src/pages/power-plumbing/petroleum-generator.astro
  - src/pages/power-plumbing/water-geyser.astro
  - src/pages/power-plumbing/cool-slush-geyser.astro
  - src/pages/power-plumbing/cool-steam-vent.astro
  - src/pages/power-plumbing/natural-gas-geyser.astro
  - src/pages/power-plumbing/hydrogen-vent.astro
  - src/pages/power-plumbing/salt-water-geyser.astro
autonomous: true
requirements:
  - POWR-01
  - POWR-02
  - POWR-03
  - POWR-04

must_haves:
  truths:
    - "User can visit power/plumbing hub and see category cards linking to generator and geyser pages"
    - "User can view battery types with capacity, leak rate, and heat output on the power hub page"
    - "User can look up any generator and see wattage, fuel type, consumption rate, heat output, and byproducts"
    - "User can look up any geyser and see output material, average output rate, temperature, biome, and taming method"
    - "Generator and geyser pages follow the critter page pattern with RanchBuildTabs and stat cards"
  artifacts:
    - path: "src/data/generators.yaml"
      provides: "4 generator entries with verified game stats"
      contains: "coal_generator"
    - path: "src/data/batteries.yaml"
      provides: "3 battery entries with capacity and heat data"
      contains: "smart_battery"
    - path: "src/data/geysers.yaml"
      provides: "6 geyser entries with output and taming data"
      contains: "water_geyser"
    - path: "src/components/BuildStatCard.astro"
      provides: "Stat card for generator and geyser pages"
      min_lines: 20
    - path: "src/pages/power-plumbing/index.astro"
      provides: "Power hub with battery section and category links"
      contains: "getCollection"
    - path: "src/content.config.ts"
      provides: "generators, batteries, geysers collections"
      contains: "generatorSchema"
  key_links:
    - from: "src/pages/power-plumbing/index.astro"
      to: "src/data/batteries.yaml"
      via: "getCollection('batteries')"
      pattern: "getCollection.*batteries"
    - from: "src/pages/power-plumbing/coal-generator.astro"
      to: "src/components/BuildStatCard.astro"
      via: "component import"
      pattern: "BuildStatCard"
    - from: "src/pages/power-plumbing/water-geyser.astro"
      to: "src/data/geysers.yaml"
      via: "getCollection('geysers')"
      pattern: "getCollection.*geysers"
---

<objective>
Power and plumbing domain: generator, battery, and geyser data layers plus hub page, 4 generator setup pages, and 6 geyser taming pages.

Purpose: Satisfy POWR-01 (generators) and POWR-02 (batteries). POWR-03 (pipes) and POWR-04 (pumps) are intentionally deferred per user decision in CONTEXT.md -- raw pipe/pump stat tables are out of scope for this phase. The power section focuses on practical build guides (generator setups, geyser taming) instead.
Output: Working power/plumbing section with generator setup pages, geyser taming pages, and battery reference on the hub.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-farming-power-plumbing/03-RESEARCH.md
@.planning/phases/03-farming-power-plumbing/03-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, content.config.ts will have cropSchema and crops collection added.
     This plan adds generatorSchema, batterySchema, geyserSchema, and their collections. -->

From src/content.config.ts (after Plan 01):
```typescript
const unitValue = z.object({
  value: z.number(),
  unit: z.string(),
});
// + cropSchema and crops collection from Plan 01
// This plan adds: generatorSchema, batterySchema, geyserSchema
export const collections = { hatches, dreckos, pacus, pips, slicksters, shineBugs, rooms, crops };
```

From src/components/RanchBuildTabs.astro:
- Accepts `family: string` prop
- Reuse directly for generator and geyser pages with unique family slugs

From src/components/StatPill.astro:
- Accepts `value` and `unit` props

From src/components/CropStatCard.astro (created in Plan 01 -- pattern to follow):
- Card layout with emoji placeholder + name + 2-col stats grid + notes
- Adapt for generator/geyser-specific fields

From src/pages/ranching/index.astro (hub pattern):
- Array of items with name + href, rendered as grid of card links
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generator, battery, and geyser data layers</name>
  <files>src/content.config.ts, src/data/generators.yaml, src/data/batteries.yaml, src/data/geysers.yaml</files>
  <action>
1. Add three new schemas to content.config.ts (after cropSchema from Plan 01):

   generatorSchema:
   - id: z.string(), name: z.string(), dlc: z.array(z.string())
   - watts: z.number() -- power output in watts
   - fuel_type: z.string() -- "coal", "natural_gas", "hydrogen", "petroleum"
   - consumption: unitValue -- e.g. {value: 1000, unit: "g/s"}
   - heat_output: unitValue -- e.g. {value: 9, unit: "kDTU/s"}
   - byproducts: z.array(z.object({ product: z.string(), rate: unitValue, temp: unitValue.optional() })).optional()
   - dimensions: z.string() -- e.g. "3x3"
   - notes: z.string().optional()
   - image_slot: z.boolean().default(true)

   batterySchema:
   - id: z.string(), name: z.string(), dlc: z.array(z.string())
   - capacity: unitValue -- e.g. {value: 10, unit: "kJ"}
   - leak_rate: unitValue -- e.g. {value: 1, unit: "kJ/cycle"}
   - heat_output: unitValue
   - dimensions: z.string()
   - notes: z.string().optional()

   geyserSchema:
   - id: z.string(), name: z.string(), dlc: z.array(z.string())
   - output_material: z.string()
   - avg_output: unitValue -- labeled as average in display
   - output_temp: unitValue
   - biome: z.string()
   - taming_complexity: z.enum(['low', 'medium', 'high'])
   - taming_method: z.string()
   - power_output: unitValue.optional() -- for geysers that can generate power
   - notes: z.string().optional()
   - image_slot: z.boolean().default(true)

2. Define collections:
   ```
   const generators = defineCollection({ loader: file('src/data/generators.yaml'), schema: generatorSchema });
   const batteries = defineCollection({ loader: file('src/data/batteries.yaml'), schema: batterySchema });
   const geysers = defineCollection({ loader: file('src/data/geysers.yaml'), schema: geyserSchema });
   ```

3. Add to exports: `export const collections = { ...existing, generators, batteries, geysers };`

4. Create src/data/generators.yaml with 4 generators. Use exact data from 03-RESEARCH.md "Generator YAML Data" section: Coal Generator (600W), Natural Gas Generator (800W), Hydrogen Generator (800W), Petroleum Generator (2000W). Include byproducts arrays where applicable.

5. Create src/data/batteries.yaml with 3 batteries. Use exact data from 03-RESEARCH.md: Battery (10kJ), Smart Battery (20kJ), Jumbo Battery (40kJ).

6. Create src/data/geysers.yaml with 6 geysers. Use exact data from 03-RESEARCH.md: Water Geyser, Cool Slush Geyser, Cool Steam Vent, Natural Gas Geyser, Hydrogen Vent, Salt Water Geyser. Label avg_output values clearly as averages in notes.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 YAML files validate against Zod schemas, content.config.ts exports generators/batteries/geysers collections, build passes</done>
</task>

<task type="auto">
  <name>Task 2: BuildStatCard component, power hub page, and 4 generator pages</name>
  <files>src/components/BuildStatCard.astro, src/pages/power-plumbing/index.astro, src/pages/power-plumbing/coal-generator.astro, src/pages/power-plumbing/natural-gas-generator.astro, src/pages/power-plumbing/hydrogen-generator.astro, src/pages/power-plumbing/petroleum-generator.astro</files>
  <action>
1. Create BuildStatCard.astro -- a flexible stat card that works for both generators and geysers:
   - Props: `entry` object with `data` containing the fields, `mode: 'generator' | 'geyser'`
   - Card layout: border border-oni-bg-elevated rounded-xl p-5 (same as CritterVariantCard)
   - Top section: emoji placeholder (use a lightning bolt for generators, a water/volcano emoji for geysers) + name
   - Generator mode stats grid (2-col):
     * Power Output: StatPill with watts + "W"
     * Fuel: fuel_type formatted with formatName helper
     * Consumption: StatPill with consumption value + unit
     * Heat Output: StatPill with heat_output value + unit
     * Dimensions: display string
     * Byproducts: list each byproduct with rate StatPill, guard optional field
     * DLC: pill badges
   - Geyser mode stats grid (2-col):
     * Output Material: formatted name
     * Avg Output: StatPill with avg_output value + unit, label as "Avg Output" (not just "Output")
     * Output Temperature: StatPill with output_temp
     * Biome: formatted name
     * Taming Complexity: display as colored badge (low=green, medium=yellow, high=red using oni tokens)
     * Taming Method: text description
     * Power Output: StatPill if present (optional field), omit section if null
     * DLC: pill badges
   - Notes section at bottom (same pattern)
   - Include formatName helper (same as CritterVariantCard)

2. Rewrite src/pages/power-plumbing/index.astro (replace stub):
   - Import BaseLayout, StatPill, getCollection
   - Fetch batteries: `const batteries = await getCollection('batteries');`
   - Page heading: "Power / Plumbing" with subtitle "Generators, batteries, and geyser taming -- wattage and throughput at a glance."
   - Section 1 -- "Generators" heading with grid of card links (same style as ranching hub) to 4 generator pages:
     * Coal Generator -> /oni-fanpage/power-plumbing/coal-generator/
     * Natural Gas Generator -> /oni-fanpage/power-plumbing/natural-gas-generator/
     * Hydrogen Generator -> /oni-fanpage/power-plumbing/hydrogen-generator/
     * Petroleum Generator -> /oni-fanpage/power-plumbing/petroleum-generator/
   - Section 2 -- "Batteries" heading with battery stat cards rendered inline (no separate page needed). For each battery, show a compact card with: name, capacity (StatPill), leak rate (StatPill), heat output (StatPill), dimensions. Use the same card border styling. This satisfies POWR-02.
   - Section 3 -- "Geysers" heading with grid of card links to 6 geyser pages:
     * Water Geyser -> /oni-fanpage/power-plumbing/water-geyser/
     * Cool Slush Geyser -> /oni-fanpage/power-plumbing/cool-slush-geyser/
     * Cool Steam Vent -> /oni-fanpage/power-plumbing/cool-steam-vent/
     * Natural Gas Geyser -> /oni-fanpage/power-plumbing/natural-gas-geyser/
     * Hydrogen Vent -> /oni-fanpage/power-plumbing/hydrogen-vent/
     * Salt Water Geyser -> /oni-fanpage/power-plumbing/salt-water-geyser/

3. Create 4 generator pages. Each follows the critter page template:
   - Frontmatter: import BaseLayout, RanchBuildTabs, BuildStatCard, getCollection
   - Fetch generators: `const allGenerators = await getCollection('generators');`
   - Filter: `const generator = allGenerators.find(g => g.data.id === '{generator_id}');`
   - Page structure: Breadcrumb (Power / Plumbing > {Name}) -> h1 heading -> RanchBuildTabs(family="{page-slug}") -> BuildStatCard(entry=generator, mode="generator")
   - Page title: "{Generator Name} -- ONI Reference"
   - File-to-id mapping:
     * coal-generator.astro -> id: coal_generator
     * natural-gas-generator.astro -> id: natural_gas_generator
     * hydrogen-generator.astro -> id: hydrogen_generator
     * petroleum-generator.astro -> id: petroleum_generator
   - All links use /oni-fanpage/ base path
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Power hub shows generators/batteries/geysers sections, battery stats render inline on hub, all 4 generator pages build with stat cards and build tabs</done>
</task>

<task type="auto">
  <name>Task 3: 6 geyser taming pages</name>
  <files>src/pages/power-plumbing/water-geyser.astro, src/pages/power-plumbing/cool-slush-geyser.astro, src/pages/power-plumbing/cool-steam-vent.astro, src/pages/power-plumbing/natural-gas-geyser.astro, src/pages/power-plumbing/hydrogen-vent.astro, src/pages/power-plumbing/salt-water-geyser.astro</files>
  <action>
Create 6 geyser pages. Each follows the same critter page template as generator pages:

1. Frontmatter:
   - Import BaseLayout, RanchBuildTabs, BuildStatCard, getCollection
   - Fetch geysers: `const allGeysers = await getCollection('geysers');`
   - Filter: `const geyser = allGeysers.find(g => g.data.id === '{geyser_id}');`

2. Page structure:
   - Breadcrumb: Power / Plumbing > {Geyser Name} (link to /oni-fanpage/power-plumbing/)
   - h1 with geyser name
   - RanchBuildTabs with family="{page-slug}" (unique per page to prevent radio input collisions)
   - BuildStatCard with entry=geyser, mode="geyser"

3. File-to-id mapping:
   - water-geyser.astro -> id: water_geyser
   - cool-slush-geyser.astro -> id: cool_slush_geyser
   - cool-steam-vent.astro -> id: cool_steam_vent
   - natural-gas-geyser.astro -> id: natural_gas_geyser
   - hydrogen-vent.astro -> id: hydrogen_vent
   - salt-water-geyser.astro -> id: salt_water_geyser

4. Page title: "{Geyser Name} -- ONI Reference"

5. All links use /oni-fanpage/ base path prefix.

6. Each page should be self-contained -- follow the exact same pattern as generator pages but with mode="geyser" on BuildStatCard.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Desktop/oni-fanpage && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 geyser pages build successfully, each shows breadcrumb + heading + build tabs + geyser stat card with avg output labeled, navigable from power hub</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- Power hub at /power-plumbing/ shows three sections: Generators (4 links), Batteries (3 stat cards inline), Geysers (6 links)
- Each generator page shows wattage, fuel type, consumption, heat output, byproducts
- Battery stats on hub show capacity, leak rate, heat output for all 3 types
- Each geyser page shows output material, avg output (labeled as average), temperature, biome, taming complexity, taming method
- No "null" text visible on any rendered page
- POWR-03 and POWR-04 intentionally not implemented (deferred per user decision)
</verification>

<success_criteria>
- 4 generator, 3 battery, 6 geyser data entries validated by Zod schemas
- BuildStatCard renders correctly in both generator and geyser modes
- Power hub replaces placeholder with live sections
- All 4 generator and 6 geyser detail pages follow critter page template
- Battery reference visible directly on power hub page
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-farming-power-plumbing/03-02-SUMMARY.md`
</output>
