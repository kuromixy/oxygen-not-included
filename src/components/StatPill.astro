---
interface Props {
  value: string | number;
  unit?: string;
}

const { value, unit } = Astro.props;
---

<span class="inline-flex items-center px-2 py-0.5 rounded text-sm font-mono bg-oni-stat-pill-bg text-oni-stat-pill-text">
  {value}{unit ? ` ${unit}` : ''}
</span>
